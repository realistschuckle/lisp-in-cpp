#ifndef TOKENTYPE_HPP
#define TOKENTYPE_HPP

enum TokenType {
  TOKEN_EOF,
  TOKEN_NIL,
  TOKEN_DOT,
  TOKEN_SYMBOL,
  TOKEN_NUMBER,
  TOKEN_OPEN_PAREN,
  TOKEN_CLOSE_PAREN,
  TOKEN_TICK,
  TOKEN_BACKTICK,
  TOKEN_COMMA,
  TOKEN_AT
};

#endif
